module Fractal.OpenApi.Codegen.THSpec (spec) where

import Test.Hspec

spec :: Spec
spec = do
  describe "Template Haskell Codegen" $ do
    it "placeholder - needs codegen implementation" $ do
      -- TODO: Add codegen tests
      True `shouldBe` True
