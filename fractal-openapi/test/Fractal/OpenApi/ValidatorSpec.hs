module Fractal.OpenApi.ValidatorSpec (spec) where

import Test.Hspec

spec :: Spec
spec = describe "OpenAPI Validator" $ do
  it "placeholder test" $ do
    -- TODO: Implement OpenAPI validation tests
    (1 :: Int) `shouldBe` (1 :: Int)

