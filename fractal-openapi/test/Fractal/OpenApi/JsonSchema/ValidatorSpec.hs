module Fractal.OpenApi.JsonSchema.ValidatorSpec (spec) where

import Test.Hspec

spec :: Spec
spec = do
  describe "JSON Schema Validator" $ do
    it "placeholder - needs validator implementation" $ do
      -- TODO: Add validator tests
      True `shouldBe` True
