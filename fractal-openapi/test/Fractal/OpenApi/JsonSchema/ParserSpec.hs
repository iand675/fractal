module Fractal.OpenApi.JsonSchema.ParserSpec (spec) where

import Test.Hspec

spec :: Spec
spec = do
  describe "JSON Schema Parser" $ do
    it "placeholder - needs parser implementation" $ do
      -- TODO: Add parser tests
      True `shouldBe` True
