module Fractal.OpenApi.JsonSchema.VocabularySpec (spec) where

import Test.Hspec

spec :: Spec
spec = do
  describe "Vocabulary System" $ do
    it "placeholder - needs vocabulary implementation" $ do
      -- TODO: Add vocabulary tests
      True `shouldBe` True
